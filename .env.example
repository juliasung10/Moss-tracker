# Copy to .env.local for local development. Neither is required locally: with no
# TURSO_DATABASE_URL the app just uses a plain file at data/moss.db.

# From `turso db show moss-tracker --url` (starts with libsql://)
TURSO_DATABASE_URL=

# From `turso db tokens create moss-tracker`
TURSO_AUTH_TOKEN=
